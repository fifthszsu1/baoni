# 英语作文评分系统 - 阿里云部署完整指南

## 🎯 部署概述

本系统需要部署到阿里云ECS服务器，并配置HTTPS域名供小程序调用。

**架构图：**
```
小程序 → HTTPS(443) → Nginx → 后端API → MySQL(3307)
```

## 📋 前期准备

### 1. 阿里云资源
- **ECS服务器**：推荐2核4G，CentOS 7.x或Ubuntu 18.04+
- **域名**：已备案的域名（小程序必需）
- **SSL证书**：可在阿里云申请免费SSL证书

### 2. 必需软件
```bash
# 安装Docker和Docker Compose
curl -fsSL https://get.docker.com | bash -s docker
sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# 启动Docker服务
sudo systemctl start docker
sudo systemctl enable docker
```

## 🔧 部署步骤

### 第一步：上传代码
```bash
# 将项目代码上传到服务器
scp -r ./baoni root@your-server-ip:/opt/
cd /opt/baoni
```

### 第二步：配置环境变量
```bash
# 创建环境变量文件
cat > .env << EOF
# Azure OpenAI配置
AZURE_OPENAI_API_KEY=your_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com/
AZURE_DEPLOYMENT_NAME=gpt-4
AZURE_API_VERSION=2024-02-01

# 数据库配置
MYSQL_ROOT_PASSWORD=YourSecurePassword123!
MYSQL_PASSWORD=YourDBPassword456!
SECRET_KEY=YourSecretKey789!

# 域名配置
DOMAIN_NAME=your-domain.com
EOF
```

### 第三步：配置SSL证书
```bash
# 将SSL证书文件上传到nginx/ssl/目录
# cert.pem - SSL证书
# key.pem - SSL私钥

# 或者生成自签名证书（仅用于测试）
openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout nginx/ssl/key.pem \
  -out nginx/ssl/cert.pem \
  -subj "/C=CN/ST=Beijing/L=Beijing/O=Company/CN=your-domain.com"
```

### 第四步：配置安全组
在阿里云ECS控制台配置安全组规则：

| 规则方向 | 端口范围 | 授权对象 | 描述 |
|----------|----------|----------|------|
| 入方向 | 80/80 | 0.0.0.0/0 | HTTP访问 |
| 入方向 | 443/443 | 0.0.0.0/0 | HTTPS访问 |
| 入方向 | 22/22 | 你的IP | SSH访问 |
| 入方向 | 3307/3307 | 127.0.0.1/32 | MySQL（可选） |

### 第五步：部署系统
```bash
# 设置环境变量
export DOMAIN_NAME=your-domain.com
export AZURE_OPENAI_API_KEY=your_api_key_here

# 运行部署脚本
chmod +x deploy-production.sh
./deploy-production.sh
```

### 第六步：验证部署
```bash
# 检查服务状态
docker-compose -f docker-compose.prod.yml ps

# 测试API连接
curl https://your-domain.com/health
curl https://your-domain.com/api/miniprogram/health
```

## 🔒 小程序配置

### 1. 域名白名单配置
在微信小程序管理后台 → 开发管理 → 开发设置 → 服务器域名：

```
request合法域名：
https://your-domain.com

uploadFile合法域名：
https://your-domain.com

downloadFile合法域名：
https://your-domain.com
```

### 2. 小程序API调用示例
```javascript
// 小程序中的API基础配置
const API_BASE = 'https://your-domain.com/api/miniprogram';

// 提交作文评分
wx.uploadFile({
  url: `${API_BASE}/compositions/submit`,
  filePath: imagePath,
  name: 'image',
  formData: {
    topic_id: 1,
    scoring_rule_id: 1
  },
  success: (res) => {
    const result = JSON.parse(res.data);
    console.log('评分结果:', result);
  }
});
```

## 🔧 常见问题解决

### 问题1：小程序无法访问API
**原因**：域名未配置HTTPS或未在小程序后台添加白名单

**解决**：
1. 确保域名配置了有效的SSL证书
2. 在小程序管理后台添加域名白名单
3. 检查域名DNS解析是否正确

### 问题2：图片上传失败
**原因**：Nginx文件大小限制

**解决**：
```nginx
# 在nginx配置中增加
client_max_body_size 10M;
```

### 问题3：OCR识别失败
**原因**：缺少Tesseract OCR依赖

**解决**：
```dockerfile
# 在Dockerfile.backend中添加
RUN apt-get update && apt-get install -y tesseract-ocr tesseract-ocr-chi-sim
```

### 问题4：数据库连接失败
**原因**：字符编码或连接配置问题

**解决**：
1. 检查MySQL容器是否正常启动
2. 验证数据库连接字符串中的charset参数
3. 查看容器日志：`docker logs baoni-mysql-prod`

## 📊 监控和维护

### 日志查看
```bash
# 查看所有服务日志
docker-compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker logs baoni-backend-prod
docker logs baoni-nginx-prod
docker logs baoni-mysql-prod
```

### 数据备份
```bash
# 备份数据库
docker exec baoni-mysql-prod mysqldump -u root -p baoni_scoring_db > backup_$(date +%Y%m%d).sql

# 备份上传的文件
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz uploads/
```

### 性能监控
```bash
# 查看系统资源使用
docker stats

# 查看磁盘使用
df -h
du -sh data/mysql
```

## 🚀 扩展配置

### 负载均衡（多实例）
```yaml
# docker-compose.prod.yml中增加backend副本
backend:
  deploy:
    replicas: 3
```

### Redis缓存（可选）
```yaml
redis:
  image: redis:alpine
  container_name: baoni-redis
  restart: unless-stopped
```

### 监控告警（可选）
推荐使用阿里云云监控服务监控ECS资源使用情况。

## 📞 技术支持

如遇到问题，请按以下顺序排查：

1. **检查服务状态**：`docker-compose ps`
2. **查看日志**：`docker-compose logs [service-name]`
3. **测试网络**：`curl https://your-domain.com/health`
4. **验证配置**：检查环境变量和域名配置
5. **重启服务**：`docker-compose restart [service-name]`

---

**部署完成后，你的小程序就可以通过 `https://your-domain.com/api/miniprogram/` 访问所有API接口了！** 